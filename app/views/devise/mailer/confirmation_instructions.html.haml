%p
  #{@resource.username} 様

-if @resource.unconfirmed_email.blank?
  %p=t('mailer.confirmation.account_body')
  %p= link_to confirmation_url(@resource, confirmation_token: @token), confirmation_url(@resource, confirmation_token: @token)
- else
  %p=t('mailer.confirmation.email_body')
  %p= link_to confirmation_url(@resource, confirmation_token: @token), confirmation_url(@resource, confirmation_token: @token)

%p= @resource.inspect